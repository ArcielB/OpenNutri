# Backlog

1. Connect user suggestions from the UI button to a place that is easy to track and review.
   - Evaluate Google Keep or a better alternative.

2. Add image attachments to the suggestion flow.
   - This may change the best destination chosen in item 1.

3. Fix highlighting feature errors.

4. Improve the crawler so it collects the right papers.

5. Implement both AI systems in a test setup and track:
   - performance
   - effect on user experience

6. Fix theme behavior.
   - The UI does not automatically follow the system light/dark mode on login.
   - Papers stay in light mode even when the app is in dark mode.

7. Fix reset password flow.
   - Reset email arrives, but the link opens the website and logs the user in instead of taking them to a password reset screen.
